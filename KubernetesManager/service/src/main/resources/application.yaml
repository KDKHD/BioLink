server:
#  ssl:
#    key-store: classpath:springboot.p12
#    key-store-password: password
#    key-store-type: pkcs12
#    key-alias: springboot
#    key-password: password
  port: 8080
  forward-headers-strategy: FRAMEWORK

spring:
  datasource:
    url: jdbc:postgresql://localhost:15432/postgres
    username: postgres
    password: changeme

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      naming-strategy: org.hibernate.cfg.ImprovedNamingStrategy
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  security:
    oauth2:
      client:
        registration:
          instagram:
            clientId: 1253032755109023
            clientSecret: 1a4b346f269a649d51ba06774e419030
            redirectUri: "{baseUrl}/user_security_service/oauth2/callback/{registrationId}" # Note that facebook now mandates the use of https redirect URIs, so make sure your app supports https in production
            scope:
              - user_profile
              - user_media
            authorization-grant-type: authorization_code
            client_authentication_method: client_secret_post
          facebook:
            clientId: 316935573270353
            clientSecret: f335780772f9153cfe4e607361c78739
            redirectUri: "{baseUrl}/user_security_service/oauth2/callback/{registrationId}" # Note that facebook now mandates the use of https redirect URIs, so make sure your app supports https in production
            scope:
              - email
              - public_profile
        provider:
          instagram:
            authorizationUri: https://api.instagram.com/oauth/authorize
            tokenUri: https://api.instagram.com/oauth/access_token
            userInfoUri: https://graph.instagram.com/me?fields=id,username
            authenticationMethod: query
            userNameAttribute: id
          facebook:
            authorizationUri: https://www.facebook.com/v3.0/dialog/oauth
            tokenUri: https://graph.facebook.com/v3.0/oauth/access_token
            userInfoUri: https://graph.facebook.com/v3.0/me?fields=id,first_name,middle_name,last_name,name,email,verified,picture.width(250).height(250)
app:
  auth:
    tokenSecret: 04ca023b39512e46d0c2cf4b48d5aac61d34302994c87ed4eff225dcf3b0a218739f3897051a057f9b846a69ea2927a587044164b7bae5e1306219d50b588cb1
    tokenExpirationMsec: 864000000
  cors:
    allowedOrigins: https://heezy.dev/ # Comma separated list of allowed origins
  oauth2:
    # After successfully authenticating with the OAuth2 Provider,
    # we'll be generating an auth token for the user and sending the token to the
    # redirectUri mentioned by the client in the /oauth2/authorize request.
    # We're not using cookies because they won't work well in mobile clients.
    authorizedRedirectUris:
      - https://heezy.dev/oauth2/redirect
      - https://heezy.dev/oauth2/redirect
      - myandroidapp://oauth2/redirect
      - myiosapp://oauth2/redirect